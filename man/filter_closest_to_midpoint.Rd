% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_filtering.R
\name{filter_closest_to_midpoint}
\alias{filter_closest_to_midpoint}
\title{Filter pSILAC data to keep only timepoints closest to Fraction_Old = 0.5}
\usage{
filter_closest_to_midpoint(
  data,
  n_timepoints = 2,
  target_value = 0.5,
  return_all_rows = T,
  value_col = "Fraction_Old",
  variables_col = "variables",
  observations_col = "observations",
  pulse_time_col = "Pulse_time",
  group_cols = c()
)
}
\arguments{
\item{data}{A data frame containing pSILAC data}

\item{n_timepoints}{Number of timepoints to keep (default: 2)}

\item{target_value}{Target Fraction_Old value to find closest timepoints to (default: 0.5)}

\item{return_all_rows}{If TRUE, returns all rows with a 'fit' column indicating filter status.
If FALSE, returns only rows that pass the filter (default: TRUE)}

\item{value_col}{Name of the column containing the values to filter (default: "Fraction_Old")}

\item{variables_col}{Name of the column containing variable identifiers (default: "variables")}

\item{observations_col}{Name of the column containing observation identifiers (default: "observations")}

\item{pulse_time_col}{Name of the column containing pulse time information (default: "Pulse_time")}

\item{group_cols}{Character vector of additional grouping column names (default: c())}
}
\value{
A data frame with the same structure as input, either filtered or with a 'fit' column
}
\description{
Selects the 2 timepoints per variable and group whose mean Fraction_Old values
are closest to 0.5. This region often provides the most informative data for
kinetic analysis as it has the best signal-to-noise ratio for rate determination.
}
\examples{
\dontrun{
# Keep 2 timepoints closest to 0.5 (default)
filtered_data <- filter_closest_to_midpoint(data)

# Keep 3 timepoints closest to 0.5
filtered_data <- filter_closest_to_midpoint(data, n_timepoints = 3)

# Target a different value (e.g., 0.3)
filtered_data <- filter_closest_to_midpoint(data, target_value = 0.3)
}
}
